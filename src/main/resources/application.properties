# Server
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://interchange.proxy.rlwy.net:27504/railway
spring.datasource.username=root
spring.datasource.password=IBVZNHqiuGFNItElpmKqpLccWcPMOEpM
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# File Storage Path
app.upload.dir=./uploads

# JWT
app.jwt.secret=YourSuperSecretJWTKeyThatIsAtLeast256BitsLongForHS256AlgorithmUsedInJWTSigning
app.jwt.expiration=86400000
spring.main.allow-circular-references=true

# Python AI Engine
app.ai.engine.url=http://localhost:8000

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Logging
logging.level.com.dataanalyst=DEBUG
logging.level.org.springframework.security=WARN
